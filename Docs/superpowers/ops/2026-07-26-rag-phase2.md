# RAG Phase 2 — ops (2026-07-26)

## Decisões
1. Ingestão do zero — `TRUNCATE eros_knowledge_chunks` feito; sem reaproveitar rows.
2. Sem fallback lexical — ask exige hits vetoriais.
3. Tenant só JWT (`app_metadata.company_id`) — sem `x-company-id`.
4. Embed default: **voyage-4-large** @ 1024.

## Voyage API key
1. Conta: [https://www.voyageai.com](https://www.voyageai.com) ou Atlas → **AI Models**.
2. Dashboard → **API keys** → **Create new secret key** ([docs](https://docs.voyageai.com/docs/api-key-and-installation)).
3. Secret Edge: `supabase secrets set VOYAGE_API_KEY=... --project-ref gxmaxbjgdrqdcizvdojp`
4. Opcional: `EMBEDDING_PROVIDER=voyage` `EMBEDDING_MODEL=voyage-4-large`

## Fluxo
1. UI **Treinar & Publicar** → `eros-knowledge-ingest` (hash + embed + insert).
2. Chat → `eros-knowledge-query` → `match_chunks` → `sources[]` na UI.
3. Chunking: [`Docs/CHUNKING_EROS.md`](../CHUNKING_EROS.md) (`chunking-v1`).

## Aceite manual
Após secret + train: *"Quais academias TotalPass com TP3?"* → resposta + fontes com `score`.
