# RAG Phase 1 — ops (2026-07-26)

**Projeto:** `gxmaxbjgdrqdcizvdojp` (controle de assistente)  
**Dimensão:** `vector(1024)` — config via `eros_config.key=embedding` ou env (`EMBEDDING_PROVIDER`, `EMBEDDING_MODEL`, `EMBEDDING_DIMENSION=1024`).

## Entregue
- Migration `supabase/migrations/20260726_rag_phase1.sql` — colunas + HNSW + `match_chunks` + backfill `tenant_id ← groups.company_id`
- Edge `eros-knowledge-query` — tenant gate 403, RPC vetorial, `<chunk>` anti-injection, `sources[]`
- `_shared/embed.ts` — embed query (openai/voyage/ollama) sem UUID hardcoded
- `knowledgeService.ask` tipa `sources`

## Env Edge (secrets)
- `OPENAI_API_KEY` (ou `VOYAGE_API_KEY` / Ollama)
- Opcional: `EMBEDDING_PROVIDER`, `EMBEDDING_MODEL`, `EMBEDDING_VERSION`, `RAG_TOP_K`, `RAG_MIN_SIMILARITY`
- `SUPABASE_ANON_KEY` — resolve `app_metadata.company_id` do JWT

## Header opcional
- `x-company-id: <uuid>` — tenant sem Auth (dev)

## Nota
Chunks existentes **sem** `embedding` → resposta usa `retrieval: "legacy_no_embedding"` até Fase 2 (train grava vetores).

## Fase 2 (aguardar OK)
Idempotência `content_hash`, `CHUNKING_EROS.md`, UI fontes, extrair `agentMissingPlans`.
